== Delete GitIgnore
Deletes the existing .gitignore file if it exists.

=== Example
[source,groovy,indent=0,subs="verbatim,attributes",role="primary"]
.Bash
----
include::{samplesCodeDir}/delete-gitignore/bash/example.sh[]
----